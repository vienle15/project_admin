interface Product {
  productImg: string;
  productName: string;
  productPrice: string;
  productQuantity: number;
  productSize: string;
  productCode: string;
  productId: string;
}
export default Product;
